<svg viewBox="0 0 320 80" fill="none" xmlns="http://www.w3.org/2000/svg" aria-label="PermitJockey">
  <!-- Same as logo.svg but white — for dark backgrounds (nav, footer, email headers) -->

  <!-- Stem -->
  <rect x="0" y="8" width="14" height="64" rx="3" fill="white"/>

  <!-- Helmet dome -->
  <path d="M 14 8 A 25 18 0 0 1 14 44 L 14 8 Z" fill="white"/>

  <!-- Visor (amber stays — it pops on dark) -->
  <path d="M 38 20 L 57 26 L 38 32 Z" fill="#f59e0b"/>

  <!-- Chin strap -->
  <path d="M 14 44 Q 26 60 40 63"
        stroke="#f59e0b" stroke-width="5"
        fill="none" stroke-linecap="round"/>

  <!-- Wordmark -->
  <text x="68" y="54"
        font-family="Inter, 'Helvetica Neue', Arial, sans-serif"
        font-size="38"
        fill="white"
        letter-spacing="-0.5">
    <tspan font-weight="400">Permit</tspan><tspan font-weight="800">Jockey</tspan>
  </text>
</svg>
